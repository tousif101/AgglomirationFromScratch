# AgglomirationFromScratch
Implement a clustering algorithm from scratch. Agglomeration. 

There is a CSV of shopping data. Uses agglomertive clustering to find the three groups of shoopers within that data. 
